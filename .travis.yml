language: python
python:
  - "3.6.4"

# command to install dependencies
install:
  - pip install -r requirements.txt

script:
  - pytest -v
  - coverage run test_app.py
  - coverage report
  - codecov
